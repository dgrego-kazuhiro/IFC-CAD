/**
 * Viewport の 3D/2D レンダリング色定義。
 * scene.addObject() に渡す color: プロパティの値をすべてここに集約する。
 */

export type RGBA4 = [number, number, number, number];

// ── 選択ハイライト ──────────────────────────────────────────────────────────
/** 選択中の要素共通色 (orange-500) */
export const SEL: RGBA4 = [249 / 255, 115 / 255, 22 / 255, 1.0];

// ── 壁 ─────────────────────────────────────────────────────────────────────
/** 壁 — 2D スケッチ表示 (非選択) */
export const WALL_2D: RGBA4   = [140 / 255, 140 / 255, 145 / 255, 0.85];
/** 壁 — 3D メッシュ (非選択) */
export const WALL_3D: RGBA4   = [0.88, 0.88, 0.9, 1.0];
/** 壁作図中の仮線・始点マーカー */
export const WALL_DRAFT: RGBA4 = [0.2, 0.2, 0.2, 1.0];

// ── スケッチ参照 (Wall / Select モード — Room スケッチの参照表示) ────────────
/** スケッチポリゴン輪郭ストローク (blue-500 系) */
export const SKETCH_INNER_STROKE: RGBA4 = [59 / 255, 130 / 255, 246 / 255, 0.9];
/** スケッチポリゴン内部塗り (blue-500 薄め) */
export const SKETCH_INNER_FILL: RGBA4   = [59 / 255, 130 / 255, 246 / 255, 0.06];
/** スケッチ外周アウトライン (slate-500 系) */
export const SKETCH_OUTLINE: RGBA4      = [100 / 255, 116 / 255, 139 / 255, 0.9];

// ── スラブ ──────────────────────────────────────────────────────────────────
/** スラブ — 通常表示 (非選択) */
export const SLAB_DEFAULT: RGBA4  = [0.9, 0.9, 0.92, 1.0];
/** スラブモードの部屋塗り (非選択) */
export const SLAB_SPACE: RGBA4    = [0.5, 0.8, 1.0, 0.55];
/** スラブ手動スケッチ線 */
export const SLAB_SKETCH: RGBA4   = [0.95, 0.55, 0.1, 1.0];
/** スラブ閉合ヒント点線 */
export const SLAB_CLOSING: RGBA4  = [0.95, 0.8, 0.3, 0.7];

// ── 柱 ─────────────────────────────────────────────────────────────────────
/** 柱 — 通常表示 (非選択) */
export const COLUMN_DEFAULT: RGBA4  = [0.85, 0.85, 0.88, 1.0];
/** 柱配置プレビュー (カーソル追従ゴースト) */
export const COLUMN_PREVIEW: RGBA4  = [0.55, 0.85, 1.0, 0.6];

// ── 梁 ─────────────────────────────────────────────────────────────────────
/** 梁 — 通常表示 (非選択) */
export const BEAM_DEFAULT: RGBA4 = [0.83, 0.83, 0.86, 1.0];
/** 梁プレビュー線 (始点→カーソル) */
export const BEAM_PREVIEW: RGBA4 = [0.1, 0.4, 0.9, 1.0];
/** 梁プレビュー 3D ゴースト */
export const BEAM_GHOST: RGBA4   = [0.55, 0.75, 1.0, 0.55];

// ── 階段 ────────────────────────────────────────────────────────────────────
/** 階段 — 通常表示 (非選択) */
export const STAIR_DEFAULT: RGBA4 = [0.85, 0.85, 0.88, 1.0];

// ── ドア ────────────────────────────────────────────────────────────────────
/** ドア — 通常表示 (非選択、木目茶系) */
export const DOOR_DEFAULT: RGBA4  = [0.55, 0.35, 0.18, 1.0];
/** ドア配置プレビュー */
export const DOOR_PREVIEW: RGBA4  = [0.9, 0.55, 0.2, 0.7];

// ── 窓 ─────────────────────────────────────────────────────────────────────
/** 窓 — 通常表示 (非選択、ガラス水色) */
export const WINDOW_DEFAULT: RGBA4 = [0.6, 0.78, 0.92, 0.85];
/** 窓配置プレビュー */
export const WINDOW_PREVIEW: RGBA4 = [0.55, 0.85, 1.0, 0.7];

// ── 通芯 ────────────────────────────────────────────────────────────────────
/** 通芯 — スケッチ選択ハイライト (エメラルド) */
export const GRID_SKETCH_SEL: RGBA4 = [0.1, 0.85, 0.35, 1.0];
